name: CI
on:
  push:
    branches: [ "master", "feature/**" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: config file transfer by scp
        uses: siva1024/scp-deployer@latest
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_PEM_KEY }}
          source: "/home/runner/work/do-it-server/do-it-server/deploy/deploy.sh /home/runner/work/do-it-server/do-it-server/deploy/docker-compose.green.yml /home/runner/work/do-it-server/do-it-server/deploy/docker-compose.red.yml"
          target: "/home/ubuntu/do-it-deploy/script/server/deploy.sh /home/ubuntu/do-it-deploy/docker/server/docker-compose.green.yml /home/ubuntu/do-it-deploy/docker/server/docker-compose.red.yml"
      - name: deploy docker image to EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_PEM_KEY }}
          script: |
            sudo docker pull ${{secrets.DOCKERHUB_USERNAME}}/${{secrets.DOCKER_REPO}}
            sudo chmod 775 do-it-deploy/script/server/deploy.sh
            BASH_RESULT=$(sh /home/ubuntu/do-it-deploy/script/server/deploy.sh | tail -1)
            echo "${BASH_RESULT}"
    
